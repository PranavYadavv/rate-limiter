[project]
name = "rate-limiter"
version = "0.1.0"
description = "Distributed rate limiting with sliding window algorithm"
requires-python = ">=3.8"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"
